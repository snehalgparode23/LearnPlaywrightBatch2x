let count =52;
let negative = -10;
let zero = 0;
let decimal = 3.14;

let h = 0xFF; // hexadecimal literal (255 in decimal)
console.log(typeof h); // Output: "number"
let color_Hex = 0x00FF00; // hexadecimal literal for green color

let octal = 0o755; //base 8 literal (493 in decimal)

let million = 1e6; // scientific notation for 1 million
let small = 1e-6; // scientific notation for 0.000001