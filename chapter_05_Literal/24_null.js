let no_Audi_Pramod = null; // This variable is intentionally set to null to indicate "no value" or "empty"
let pramod_has_Two_Wives; // This variable is declared but not assigned, so it is undefined

let age = 0;
let audi = ""; // This variable is assigned an empty string, which is a valid string literal but contains no characters
console.log(typeof audi); // Output: "string"
console.log(typeof age); // Output: "number"
